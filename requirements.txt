# keep only non-runtime deps here; GPU runtime installed explicitly in Dockerfile
onnx==1.19.1
opencv_python==4.7.0.72
# Note: `torch` and `torchvision` are installed separately to match CUDA.
# For GPU builds the Dockerfile will install a cu121 wheel (PyTorch 2.2.x + cu121).
# For CPU-only builds install CPU wheels locally or modify this file to pin torch.
